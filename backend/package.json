{
  "dependencies": {
    "axios": "^1.9.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "request": "^2.88.2",
    "xml2js": "^0.6.2"
  },
  "name": "weatherplay",
  "version": "1.0.0",
  "main": "controller.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "description": "1. `npm install` 로 기본 환경 세팅\r 2. `npm start` 로 실행",
  "devDependencies": {}
}
